$

arrays and loops

list of items/peaces of data

0 index, first item
[0] = start with 0 the list

methods

.push()
.pop()
.shift()



... express operator

_______________________________

Array.prototype.findIndex()

callback functions


const array = [5, 12, 8, 130, 44];

const isLargeNumber = (element) => element > 13;

console.log(array.findIndex(isLargeNumber));
// Expected output: 3


for words is diferent

_______________________________

stacks and 

arrays are mutable
change 
array is a list

myArr[2] = 12

//string  can be REPLACE but not mutable 


lenth 0 



push add an element to an 

slice === (string method )



length count elements, characters


array is not a tyype


write a function sudo code - ask the palindrome question
if the string is a palindrome 


web
_______________________________

regex101.com
_______________________________


normalized = str.replace

